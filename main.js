const activitiesFactory = () => {
    return {
        verb: ['play', 'go', 'watch', 'learn']
    }
}

const predictMessage = (username) => {
    if (typeof username === 'string') {
        
    } else {
        return 'incorrect name';
    }
}